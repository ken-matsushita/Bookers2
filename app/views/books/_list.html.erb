<h1>User info</h1>
<p><%= image_tag book.user.get_profile_image(100,100) %></p>
<span>name</span><%= book.user.name %>
<span>introduction</span><%= book.user.introduction %>
<div><%= link_to "Edit", edit_user_path(book.user_id) %></div>

<h1>New book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this book from being saved
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <div><%= f.submit 'Create Book' %></div>
<% end %>
